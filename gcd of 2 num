#include <stdio.h>


int computeGCD(int a, int b) {
    
    if (a < 0) a = -a;
    if (b < 0) b = -b;

  
    if (a == 0 && b == 0) {
        return 0; 
    }

    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
